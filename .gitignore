# Dependencies
node_modules/
.pnp
.pnp.js

# Next.js / build output
.next/
out/
build/
dist/

# Production static export is generated by `npm run build` — never commit it
# (see next.config.ts: output: 'export')

# TypeScript incremental build cache
*.tsbuildinfo
next-env.d.ts

# Testing / coverage
coverage/
.vitest/

# Environment & secrets
.env
.env.*
!.env.example

# Editor / OS cruft
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
*.swp
*.swo
*~

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# Real receipt photos for OCR eval — kept LOCAL ONLY (privacy: may show
# bystanders, payment lines, e-receipt QR codes). Bring your own to run
# `npm run eval:ocr`. The synthetic fixture (syn-*.png) IS committed.
tests/eval/images/real-*
tests/eval/images/*.local.*
